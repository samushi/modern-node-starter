name: CI

on: [push]

jobs:
  start:
      name: "Starting CLI"
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v1
        - uses: actions/setup-node@v1
        - run: npm ci --ignore-scripts
        - run: npm run start