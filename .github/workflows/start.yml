name: CI

on: [push]

jobs:
  start:
      name: "Starting CLI"
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-node@v16
        - run: npm run start